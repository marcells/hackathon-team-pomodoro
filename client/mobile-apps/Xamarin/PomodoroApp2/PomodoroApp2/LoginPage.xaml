<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PomodoroApp2.LoginPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Pomodoro" VerticalOptions="Center"  HorizontalOptions="Center" FontAttributes="Bold"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Column="0" Text="Enter a name:" VerticalOptions="Center" HorizontalOptions="Center"/>
                <Entry Grid.Column="1" x:Name="nameText" Text="c# ftw! ;)" HorizontalOptions="Center" />
            </Grid>

            <Button x:Name="loginButon" Text="Login" VerticalOptions="Center"  HorizontalOptions="Center" Clicked="OnLogin" BackgroundColor="Green" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>